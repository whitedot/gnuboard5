# 현재 프로젝트 종합 평가 보고서

## 1. 기본 정보

- 평가 일시: 2026-04-22
- 평가자: Codex
- 브랜치: `codex/legacy-comment-cleanup-2026-04-22`
- 커밋 해시: `259183b48d1bb0d7edc527b2a98d34aeef1603bb`
- 평가 기준 문서:
  - `docs/project-evaluation-plan.md`

## 2. 평가 결론 요약

- 최종 판정:
  - 현재 단계 안정화 우선
- 종합 점수: `4.0 / 5`
- 핵심 판단 한 줄 요약:
  - member-only 1차 목표는 대부분 달성됐지만, 수동 QA와 운영 검증 기록이 비어 있어 다음 단계 확장보다 안정화 사이클이 먼저 필요하다.

## 3. 영역별 점수

| 평가 영역 | 점수(1~5) | 근거 요약 |
|---|---:|---|
| 범위 정합성 | 4.5 | README, 범위 문서, 설치 스키마, 실제 디렉터리 구조가 전반적으로 member-only 기준과 일치한다. |
| 기능 안정성 | 4.0 | 핵심 회원/관리자 시나리오 수동 QA를 실행했고, 검증 중 발견한 진입점/페이징 이슈를 바로 보정했다. |
| 아키텍처와 유지보수성 | 4.5 | `bootstrap`, `support`, `domain/member`, `domain/admin` 경계가 실제 코드에 반영돼 있고 컨트롤러가 얇아졌다. |
| UI 일관성과 빌드 위생 | 4.0 | Tailwind source-of-truth와 빌드 스크립트가 정리돼 있고 `npm run build`가 통과한다. |
| 보안과 운영 리스크 | 4.0 | 메일 인증, 비밀번호 재설정, 관리자 회원 저장 흐름을 로컬 메일 수신 환경까지 포함해 확인했다. |
| 문서와 협업 준비도 | 4.5 | 주요 문서가 정리돼 있고 이번에 실제 수동 QA 실행 결과와 발견 이슈를 기록했다. |

## 4. 주요 발견 사항

### P1

- 없음

### P2

- 없음

### P3

- 없음

## 5. 문서/범위 정합성 점검

- README와 현재 범위가 일치하는가:
  - 대체로 일치한다. 현재 저장소는 회원, 인증, 관리자 회원 관리 중심 구조로 정리돼 있다.
- 범위 문서와 실제 파일 구성이 일치하는가:
  - 대체로 일치한다. `lib/bootstrap`, `lib/support`, `lib/domain/member`, `lib/domain/admin` 구조가 실제로 존재한다.
- 제거된 기능 참조가 설정/설치/런타임 경로에 남아 있는가:
  - 평가 시점에는 일부 흔적이 있었지만, 후속 정리로 `homepage`, `device`, `SMS`, `아이핀` 관련 활성 참조를 제거했다.
- 비고:
  - 보고서 작성 직후 같은 작업 세션에서 범위 밖 활성 코드 흔적 1차 정리를 반영했다.

## 6. 정적 코드 리뷰 메모

### 회원 흐름

- 확인 파일:
  - `member/register_form_update.php`
  - `lib/domain/member/request-register.lib.php`
  - `lib/domain/member/flow-register.lib.php`
- 구조 평가:
  - 입력 수집, 검증, 저장, 응답 흐름이 문서화된 경계에 맞게 분리돼 있다.
- 리스크:
  - 회원가입/수정 폼에서 발견된 경고와 진입점 문제는 이번 QA 세션에서 보정했다.

### 관리자 흐름

- 확인 파일:
  - `adm/member_form_update.php`
  - `adm/_common.php`
  - `lib/domain/admin/member.lib.php`
- 구조 평가:
  - 컨트롤러가 얇고 관리자 공통 부트스트랩 책임이 `lib/domain/admin` 쪽으로 모였다.
- 리스크:
  - 삭제/탈퇴 정책은 회원아이디와 상태 정보만 유지하고 나머지 개인정보와 인증 이력을 비식별 처리하는 방향으로 정리됐다.

### 공용 라이브러리

- 확인 파일:
  - `common.php`
  - `lib/common.lib.php`
  - `lib/bootstrap/runtime.lib.php`
  - `lib/support/member.lib.php`
- 책임 분리 상태:
  - 공용 초기화와 support/domain 로더 역할이 상당히 정리됐다.
- 리스크:
  - 일부 레거시 표현과 사용하지 않는 호환 흔적은 추가 정리가 필요하다.

## 7. UI/빌드 점검

- 공용 스타일 source of truth 유지 여부:
  - 유지 중. `tailwind4/` 소스와 `package.json` 빌드 스크립트가 기준 역할을 한다.
- 인라인 스타일 또는 산출물 드리프트 여부:
  - 평가 중 `npm run build`를 실행해 생성물 기준 상태를 다시 맞췄다.
- 실행한 빌드 명령:
  - `npm run build`
- 빌드 결과:
  - 통과

## 8. 운영/보안 점검

- 인증/비밀번호 재설정 검토 결과:
  - 로컬 Mailpit 환경에서 비밀번호 재설정 메일 발송, 인증 링크 처리, 신규 비밀번호 로그인까지 확인했다.
- 메일 발송 흐름 검토 결과:
  - 회원가입 이메일 인증과 회원정보 수정 후 재인증 메일 발송, 인증 링크 처리, 로그인 차단/해제까지 확인했다.
- 보안 점검 절차 확인 결과:
  - 점검 문서와 체크리스트는 있으나 운영 증적은 아직 없다.
- 운영상 주의사항:
  - 다음 단계 착수 전 메일 인증, 비밀번호 재설정, 관리자 회원 수정 경로를 수동 검증하고 결과를 남겨야 한다.

## 9. 수동 QA 실행 기록

| 시나리오 | 결과 | 실패 단계 | 실제 결과 | 재현 조건 | 비고 |
|---|---|---|---|---|---|
| 회원가입 | 통과 |  | `qauser04` 비인증 모드 가입 완료 화면과 자동 로그인 확인 | 로컬 PHP 서버 + MariaDB + Mailpit | 초기에 일부 진입점 BOM 문제를 보정한 뒤 정상 동작 |
| 로그인 / 로그아웃 | 통과 |  | 관리자 `admin`, 일반회원 `qauser04` 로그인/로그아웃과 보호 메뉴 노출 전환 확인 | 동일 | 세션과 쿠키 전환 정상 |
| 이메일 인증 | 통과 |  | `qauser05` 이메일 인증 가입, 미인증 로그인 차단, 인증 링크 처리 후 로그인 허용 확인 | Mailpit 수신 메일 사용 | `qauser04` 이메일 변경 후 재인증 흐름도 추가 확인 |
| 비밀번호 재설정 | 통과 |  | `qauser04@example.test` 로 찾기 메일 발송, `509585` 임시 비밀번호 발급, 링크 처리 후 로그인 성공 | Mailpit 수신 메일 사용 | DB의 `mb_lost_certify` 초기화까지 확인 |
| 회원정보 수정 | 통과 |  | `qauser04` 비밀번호 재확인 후 이메일 변경, 새 비밀번호 `User456!` 저장, 재인증 전 로그인 차단과 재인증 후 로그인 성공 확인 | 동일 | 회원수정 폼 hidden `url` 경고는 이번 세션에서 보정 |
| 회원탈퇴 | 통과 |  | `qauser05` 탈퇴 처리 후 세션 종료 및 재로그인 차단 확인 | 동일 | `mb_leave_date=20260422` 기록 확인 |
| 관리자 회원 CRUD | 통과 |  | `admqa01` 생성, 수정, 삭제 처리 후 로그인 불가와 계정 익명화 확인 | 관리자 세션 | 회원아이디와 상태 정보만 남기고 이름·닉네임·연락처·인증 이력을 제거하며, 관리자 UI에서는 탈퇴 아이디를 일부 마스킹해 노출 |
| 관리자 회원 목록 | 통과 |  | 목록/검색 렌더와 검색 결과 노출 확인 | 관리자 세션 | PHP 8 타입 오류를 유발하던 페이징 계산은 이번 세션에서 보정 |
| 삭제/탈퇴 개인정보 최소 보존 | 통과 |  | `policydel01`, `policyleave01` 테스트 계정으로 `mb_id` 보존, 이름/닉네임/이메일/휴대폰/IP/인증정보 비식별 처리, `g5_auth`/`g5_cert_history`/`g5_member_cert_history` 삭제 확인 | 로컬 PHP 서버 + MariaDB | `sql_table_exists()`의 MariaDB placeholder 버그를 함께 보정 |

## 10. 통과 기준 체크

- [x] 핵심 회원 시나리오가 수동 QA를 통과했고 기록이 남아 있다.
- [x] P1 또는 미해결 운영성 P2 이슈가 대량으로 남아 있지 않다.
- [x] 제거된 기능 참조가 설정, 설치, 런타임 경로에서 다시 드러나지 않는다.
- [x] 공용 스타일 변경이 Tailwind 소스 기준으로 재빌드 가능하다.
- [x] 프로젝트 문서가 현재 범위와 남은 일을 같은 기준으로 설명한다.

## 11. 후속 액션

### 즉시 수정

- 이번 QA에서 생성된 로컬 검증 보조 파일과 임시 테스트 데이터 정리 범위를 결정한다.
- Mailpit/Docker 기반 로컬 QA 절차를 문서의 재현 가능한 실행 절차로 압축한다.

### 다음 사이클

- 회원/관리자 핵심 시나리오는 현재 수동 운영 검증 기준으로 관리한다.
- 수동 QA에서 드러난 진입점 파일 BOM, 공용 include, 관리자 메뉴/페이징 회귀를 재발 방지 체크로 묶는다.

### 보류

- `community`, `shop`, `booking` 도메인 확장은 현재 단계 안정화 이후 재검토한다.
